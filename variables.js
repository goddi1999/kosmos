const colors = {
    white: '#FFFFFF', // white
    black: '#000000', // black
    transparent: 'transparent', // transparent
};

const values = {
    // Padding (adjust as needed)
    paddingBase: 16,
    containerPadding: paddingBase, // Use base padding for container
  
    // Durations (for simple animations)
    durationShort: '100ms',
    durationMedium: '200ms',
    durationLong: '300ms',
  
    // Rounded Corners (adjust based on design)
    rounded: 4,
    roundedSm: 8,
    roundedMd: 16,
  
    // Flexbox Gap (adjust based on spacing grid)
    flexBoxGap: 8, // Base gap
    flexBoxGapSm: flexBoxGap / 2, // Half base gap for smaller gaps
  
    // Font Sizes (adjust based on design system)
    fontSizeTitle: 24,
    fontSizeSubheading: 20,
    fontSizeBody: 16,
    fontSizeCaption: 12,
  
    // Margins (adjust as needed)
    marginBase: 8, // Base margin
    marginSm: marginBase / 2, // Half base margin for smaller margins
  };
  

export { colors, values };
